{% macro btn(text, url, isActive, style = 'primary') %}
    {% if isActive %}
        <a href="{{ url }}" class="btn btn-{{ style }}">{{ text }}</a>
    {% else %}
        <a href="{{ url }}" class="btn btn-link">{{ text }}</a>
    {% endif %}
{% endmacro %}